var some_global = 123 * (32 + 12);
var another_one;
var and_out_final_one = false;

fn add(a, b) do var sum = a + b;
fn sum(..e) {
	sum := 0;
	//for i in e 
	{
		sum += i;
	}
	return sum;
}

fn main(..args) {
	var test = 123;
	test += 1;
	var oopsie = nope[8 * 2];
	var oopsie2 = nope.x;

	// for {
	// 	println("This one prints.");
	// 	break;
	// 	println("But this one does not.");
	// }

	// for i := 0; i < 10; i += 1 {
	// 	println("classic for %", i);
	// }

	// for i in range(0, 10) {
	// 	println("for range %", i);
	// }

	if a {
		var a_true_variable = 2*2;
	}
	else if b {
		var btest = false;
		a = 123;
	}
	else {
		var test = "This is a string!";
	}
}